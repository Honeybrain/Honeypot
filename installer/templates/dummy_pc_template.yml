{% for i in range(num_dummy_pc) %}
  dummy_pc_{{i}}:
    build:
      context: ./modules/DummyPc
      dockerfile: Dockerfile
    networks:
      mynetwork:{% if ip_addresses_dummy_pc[i] is not none %}
        ipv4_address: {{ ip_addresses_dummy_pc[i] }}{% endif %}
{% endfor %}